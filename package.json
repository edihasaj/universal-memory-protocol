{
  "name": "@amp/core",
  "version": "0.1.0",
  "description": "Agent Memory Protocol — reference SDK and minimal server (record format, signing, store, bindings)",
  "type": "module",
  "license": "Apache-2.0",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "serve": "node --experimental-strip-types src/bin/serve.ts",
    "conformance": "node --experimental-strip-types src/bin/conformance.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@noble/curves": "^1.6.0",
    "@noble/hashes": "^1.5.0",
    "@scure/base": "^1.2.6"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
